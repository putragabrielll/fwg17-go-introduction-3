package main

import "fmt"

// chan = channel bisa mengirim dan menerima data
// chan <- = channel hanya bisa menerima data
// <- chan = channel hanya bisa mengirim data

func Try(){
	fmt.Println("sudah test!")
}